{
  "field_is_required": "{{field}} is required",
  "invalid_format": "Invalid {{field}} format",
  "validation": {
    "field_is_required": "{{field}} is required",
    "invalid_format": "Invalid {{field}} format"
  },
  "fields": {
    "email": "Email",
    "password": "Password"
  }
}